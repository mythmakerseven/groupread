import React, { useState } from 'react'
import { useDispatch, useSelector } from 'react-redux'

const GroupView = ({ group }) => {
  const [group, setGroup] = useState(null)

  const dispatch = useDispatch()

  // TODO: the rest of the component lol
}